---
main: "{{ vault_main }}"

partition_password: "{{ vault_partition_password }}"
partition_device: "{{ vault_partition_device }}"
partition_partitions: "{{ vault_partition_partitions }}"
partition_main_mount_point: "{{ vault_partition_main_mount_point }}"
partition_os: "{{ vault_partition_os }}"
partition_physical_group: "{{ vault_partition_physical_group }}"
partition_volume_group: "{{ vault_partition_volume_group }}"
partition_volumes: "{{ vault_partition_volumes }}"
partition_boot: "{{ vault_partition_boot }}"

root_password: "{{ vault_root_password }}"

mullvad_account_number: "{{ vault_mullvad_account_number }}"
openvpn_name: "{{ vault_openvpn_name }}"

users: "{{ vault_users }}"

ssh_keys: "{{ vault_ssh_keys }}"

system_time_zone: "{{ vault_system_time_zone }}"
system_hostname: "{{ vault_system_hostname }}"

github_access_token: "{{ vault_github_access_token }}"

media_remoteserver: "{{ vault_media_remoteserver }}"
media_user: "{{ vault_media_user }}"
media_password: "{{ vault_media_password }}"

backup_main_user: "{{ vault_backup_main_user }}"
backup_final_destination: "{{ vault_backup_final_destination }}"
backup_secondary_destination: "{{ vault_backup_secondary_destination }}"
backup_configurations: "{{ vault_backup_configurations }}"
