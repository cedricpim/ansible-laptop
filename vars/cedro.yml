---
wallpaper: "{{ vault_wallpaper }}"

git_full_name: "{{ vault_git_full_name }}"
git_email: "{{ vault_git_email }}"
git_signingkey: "{{ vault_git_signingkey }}"

newsblur_username: "{{ vault_newsblur_username }}"
newsblur_password: "{{ vault_newsblur_password }}"

personal_server: "{{ vault_personal_server }}"
personal_server_shortcut: "{{ vault_personal_server_shortcut }}"
personal_server_photos: "{{ vault_personal_server_photos }}"
personal_local_photos: "{{ vault_personal_local_photos }}"
personal_public_files: "{{ vault_personal_public_files }}"

calendar_host: "{{ vault_calendar_host }}"
calendar_server: "{{ vault_calendar_server }}"
calendar_username: "{{ vault_calendar_username }}"
calendar_password: "{{ vault_calendar_password }}"
calendar_contacts: "{{ vault_calendar_contacts }}"
calendar_calendars: "{{ vault_calendar_calendars }}"

offlineimap_email_server: "{{ vault_offlineimap_email_server }}"
offlineimap_email_accounts: "{{ vault_offlineimap_email_accounts }}"

neomutt_email_server: "{{ vault_neomutt_email_server }}"
neomutt_email_accounts: "{{ vault_neomutt_email_accounts }}"
neomutt_sender_full_name: "{{ vault_neomutt_sender_full_name }}"
neomutt_mailboxes: "{{ vault_neomutt_mailboxes }}"

notmuch_full_name: "{{ vault_notmuch_full_name }}"
notmuch_primary_email: "{{ vault_notmuch_primary_email }}"
notmuch_other_emails: "{{ vault_notmuch_other_emails }}"

ledger_personal_salt: "{{ vault_ledger_personal_salt }}"
ledger_personal_password: "{{ vault_ledger_personal_password }}"
ledger_personal_categories: "{{ vault_ledger_personal_categories }}"
ledger_personal_currencies: "{{ vault_ledger_personal_currencies }}"
ledger_personal_function: "{{ vault_ledger_personal_function }}"
ledger_personal_reoccurrences: "{{ vault_ledger_personal_reoccurrences }}"

todo_main_directory: "{{ vault_todo_main_directory }}"

backup_size: "{{ vault_backup_size }}"
backup_password: "{{ vault_backup_password }}"
backup_dirs: "{{ vault_backup_dirs }}"
backup_exclude_options: "{{ vault_backup_exclude_options }}"
