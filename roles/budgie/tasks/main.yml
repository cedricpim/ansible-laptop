---
- name: "install Budgie"
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - "budgie-desktop"
    - "gnome"
    - "seahorse"
    - "gnome-tweak-tool"
    - "gnome-keyring"
    - "ntfs-3g"
    - "xcape"
  become: yes

- include_role:
    name: "pacaur"
    tasks_from: "install"
  with_items:
    - "watchdog"
    - "gnome-software-packagekit-plugin"
  loop_control:
    loop_var: "pkg_name"
  become: yes
  become_user: "{{ users[0].name }}"

- name: "enable system services"
  systemd:
    name: "{{ item }}"
    enabled: yes
    state: "started"
  with_items:
    - "gdm"
    - "watchdog"
    - "fstrim.timer"
  become: yes
