---
- name: install zsh
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - zsh
    - zsh-completions

- name: ensure pacaur is installed
  include_role:
    name: pacaur

- include_role:
    name: pacaur
    tasks_from: install_package
  vars:
    pkg_name: zsh-autosuggestions
