---
- name: install zsh
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - zsh
    - zsh-completions

- include_role:
    name: pacaur_package
  vars:
    pkg_name: zsh-autosuggestions
