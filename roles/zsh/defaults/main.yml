---
zprezto_dest: "{{ zsh_directory }}/.zprezto"
zsh_directory: "~/.config/zsh"
terminal_configuration: "~/.config/alacritty/alacritty.toml"
terminal_term: "alacritty"
prompt_theme_directory: "/usr/share/oh-my-posh/themes"
prompt_theme: "emodipt-extend.omp.json"

gnupg_directory: "~/.local/share/gnupg"

production_commands:
  - "ssh"
  - "scp"
