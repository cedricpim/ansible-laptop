---
- name: Install network tools
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - iw
    - wpa_supplicant
    - networkmanager
    - ntp
  become: yes

- name: start service for NTP
  systemd:
    name: ntpd
    state: started
    enabled: yes
  become: yes
