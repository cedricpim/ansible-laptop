---
- name: "Reload NetworkManager configuration"
  systemd:
    name: "NetworkManager"
    state: "reloaded"
  become: yes

- name: "Rebuild resolvconf configuration"
  command: "resolvconf -u"
  become: yes
