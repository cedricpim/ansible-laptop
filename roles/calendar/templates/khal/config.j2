{{ ansible_managed | comment }}

[calendars]
{% for calendar in dav_calendars %}
  [[{{ calendar.name }}]]
    path = {{ vdirsyncer_directory }}/calendars/{{ personal_server_name }}/{{ calendar.name }}/
    color = "{{ calendar.color }}"
    readonly = {{ calendar.readonly }}
{% endfor %}

[default]
  default_calendar = {{ dav_calendars[0].name }}
  highlight_event_days = true

[sqlite]
  path = {{ khal_directory }}/cache

[locale]
  unicode_symbols = False

[view]
  event_view_always_visible = True
