---
- name: "ensure ruby is installed"
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - "git"
    - "ruby"
  become: yes

- import_tasks: build.yml
- import_tasks: configuration.yml
- import_tasks: recurring.yml
  when: ledger_personal_reoccurrences is defined
