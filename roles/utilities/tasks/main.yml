---
- name: install shell utilities
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - git
    - openssh
    - easy-rsa
    - rsync
    - xsel
    - xdotool
    - wmctrl
    - ripgrep
    - ntfs-3g
    - htop
