---
- name: install terminal utilities
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - openssh
    - easy-rsa
    - rsync
    - xsel
    - xdotool
    - wmctrl
    - ripgrep
  become: yes

- include_role:
    name: pacaur_package
  vars:
    pkg_name: diff-so-fancy

- name: create folder for scripts
  file:
    path: "{{ scripts_directory }}"
    state: directory

- name: copy scripts files
  template:
    src: "{{ item }}"
    dest: "{{ scripts_directory }}/{{ (item | basename | splitext)[0] }}"
    mode: 0700
  with_fileglob: ../templates/scripts/*.j2

- import_tasks: git.yml
- import_tasks: htop.yml
- import_tasks: urlview.yml
- import_tasks: tmux.yml
- import_tasks: neovim.yml
- import_tasks: neofetch.yml
- import_tasks: urxvt.yml
