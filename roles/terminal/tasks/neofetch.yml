---
- include_role:
    name: pacaur_package
  vars:
    pkg_name: neofetch

- include_role:
    name: dotfiles
    tasks_from: symlink
  vars:
    src: "{{ neofetch_directory }}"
    create_directory: yes
