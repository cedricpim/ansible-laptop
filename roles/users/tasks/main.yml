---
- name: create staff group
  group:
    name: staff
    state: present

- name: create user "{{ user_name }}"
  user:
    name: "{{ user_name }}"
    password: "{{ user_password }}"
    createhome: yes
    groups:
      - wheel
      - staff
    append: yes
    state: present
