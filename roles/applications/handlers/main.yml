---
- name: "sysctl configured"
  command: "sysctl -p"
  become: yes

- name: "restart transmission"
  systemd:
    name: "transmission"
    state: restarted
  become: yes

- name: "restart syncthing"
  systemd:
    name: "syncthing"
    state: restarted
    enabled: yes
    user: yes
