---
- name: "Install Opensnitch and dependencies"
  pacman:
    name:
      - "opensnitch"
      - "python-pyasn"
    state: "present"
  become: yes

- name: "Enable/start opensnitchd"
  systemd:
    name: "opensnitchd"
    state: "started"
    enabled: yes
  become: yes
