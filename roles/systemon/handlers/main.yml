---
- name: "Restart Exim"
  systemd:
    name: "exim"
    state: "restarted"
  become: yes

- name: "Restart Systemon"
  systemd:
    name: "systemon"
    state: "restarted"
  become: yes

- name: "Reload daemon"
  systemd:
    daemon_reload: yes
  become: yes
