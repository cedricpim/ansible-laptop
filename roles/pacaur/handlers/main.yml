---
- name: "Cleanup cower build directory"
  file:
    path: "{{ cower_folder }}"
    state: "absent"

- name: "Cleanup pacaur build directory"
  file:
    path: "{{ pacaur_folder }}"
    state: "absent"
