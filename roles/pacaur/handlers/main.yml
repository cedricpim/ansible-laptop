---
- name: cleanup temporary directories used to install pacaur
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "{{ cower_folder }}"
    - "{{ pacaur_folder }}"
  become_user: "{{ user_name }}"
