---
- name: install fonts
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - ttf-dejavu
    - ttf-liberation

- include_role:
    name: pacaur_package
  vars:
    pkg_name: ttf-fira-code

- include_role:
    name: pacaur_package
  vars:
    pkg_name: nerd-fonts-complete
