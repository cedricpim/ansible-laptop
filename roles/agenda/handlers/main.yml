---
- name: "restart vdirsyncer timer"
  systemd:
    name: "vdirsyncer.timer"
    state: "restarted"
    scope: "user"
    daemon_reload: yes

- name: "discover vdirsyncer"
  shell: "yes | vdirsyncer discover"
