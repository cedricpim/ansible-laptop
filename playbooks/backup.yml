---
- hosts: local

  vars:
    user: "{{ lookup('env', 'USER') }}"

  vars_files:
    - "../vaulted_vars/{{ user }}/vault.yml"
    - "../vars/{{ user }}.yml"

  roles:
    - { role: "backup", tags: "backup" }
