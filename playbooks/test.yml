---
- hosts: localhost-test
  become: yes
  roles:
    - { role: "settings", tags: "settings" }
    - { role: "sudo", tags: "sudo" }
    - { role: "users", tags: "users" }
    - { role: "nvidia", tags: "nvidia" }
