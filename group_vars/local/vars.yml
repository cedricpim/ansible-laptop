---
user_name: "{{ vault_user_name }}"
user_password: "{{ vault_user_password }}"
git_full_name: "{{ vault_git_full_name }}"
git_email: "{{ vault_git_email }}"
system_time_zone: "{{ vault_system_time_zone }}"
system_hostname: "{{ vault_system_hostname }}"
